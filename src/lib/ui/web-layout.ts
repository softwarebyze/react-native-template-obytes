/** Top inset below the transparent native header on web (no safe-area API). */
export const WEB_HEADER_INSET = 56;

/** Extra top padding for settings lists under a standard header on web. */
export const WEB_SETTINGS_TOP_PADDING = 12;
